local_costmap:
  # try setting global frame from /odom to /map
  global_frame: /map
  robot_base_frame: /base
  update_frequency: 6.0
  publish_frequency: 2.0
  transform_tolerance: 0.9
  static_map: false
  rolling_window: true
  width: 10.0
  height: 10.0
  resolution: 0.05
  map_type: costmap
